[build]
builder = "RAILPACK"
buildCommand = "npm install && npx vite build && npx esbuild server.ts --platform=node --bundle --format=esm --outdir=dist --loader:.ts=ts --packages=external"

[deploy]
startCommand = "node dist/server.js"
healthcheckPath = "/"
healthcheckTimeout = 60
